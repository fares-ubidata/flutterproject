name : CI/CD
on: push


jobs:
  setup:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      # - uses: dart-lang/setup-dart@v1
      - run: ./test.sh
      - run: curl https://jsonplaceholder.typicode.com/posts
      - run: curl http://127.0.0.1:8081/repository/flutter-apk

  # test:
  #   runs-on: windows-latest
  #   needs: setup
  #   steps: 
  #     - run: ./test.sh